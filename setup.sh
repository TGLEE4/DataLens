pip install matplotlib seaborn scipy scikit-learn textblob pandas numpy
```

Then create another new file called `.streamlit/config.toml` — click **Add file → Create new file** and type the name as:
```
.streamlit/config.toml
